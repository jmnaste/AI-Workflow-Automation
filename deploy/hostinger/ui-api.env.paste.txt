# Paste these KEY=VALUE lines into the Hostinger Compose Environment panel

# UI + API host (same-origin)
PRIMARY_HOST=console.flovify.ca
UI_HOST=console.flovify.ca

# Name of the existing Traefik Docker network (from your root Traefik+n8n project)
TRAEFIK_NETWORK=root_default

# (Optional) ACME email used by Traefik; configured on the Traefik service
# SSL_EMAIL=you@example.com
