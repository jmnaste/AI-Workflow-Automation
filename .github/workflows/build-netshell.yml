name: Build and publish NetShell image

on:
  push:
    branches: [ main ]
    paths:
      - 'netshell/**'
      - '.github/workflows/build-netshell.yml'
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

jobs:
  build-netshell:
    name: Build NetShell image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract NetShell metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/jmnaste/ai-workflow-automation/netshell
          tags: |
            type=ref,event=branch
            type=sha
            type=raw,value=main

      - name: Build and push NetShell
        id: build
        uses: docker/build-push-action@v6
        with:
          context: ./netshell
          push: true
          provenance: false
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Publish build summary
        if: always()
        run: |
          {
            echo "### NetShell image published";
            echo "";
            echo "Repository: ghcr.io/jmnaste/ai-workflow-automation/netshell";
            echo "";
            echo "Tags:";
            echo "${{ steps.meta.outputs.tags }}" | sed 's/^/- /';
            echo "";
            echo "Digest: `${{ steps.build.outputs.digest }}`";
          } >> "$GITHUB_STEP_SUMMARY"
