# Auth Service Environment Variables

# Database connection
DATABASE_URL=postgresql://app_root:your_password@postgres:5432/app_db

# JWT configuration
JWT_SECRET=your-secret-key-here-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRY_MINUTES=60

# Admin token for creating users
ADMIN_TOKEN=your-admin-token-here

# OTP delivery (choose SMS or Email)
# SMS via Twilio
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Email via SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@flovify.ca

# OAuth Configuration - Microsoft 365
MICROSOFT_CLIENT_ID=your-azure-app-client-id
MICROSOFT_CLIENT_SECRET=your-azure-app-client-secret
MICROSOFT_REDIRECT_URI=http://localhost:8000/auth/oauth/ms365/callback

# OAuth Encryption Key (generate with: openssl rand -base64 32)
OAUTH_ENCRYPTION_KEY=your-base64-encoded-32-byte-key

# Service-to-Service Authentication
SERVICE_SECRET=your-shared-secret-between-auth-and-api
