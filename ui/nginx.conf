server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Serve static SPA.
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Minimal proxy: UI -> private API on Docker network
  # Use ^~ to force prefix match and avoid SPA fallback catching /api/* paths
  # location ^~ /api/ {
  #   proxy_pass http://api:8000;
  # }
  # Ensure /api (no trailing slash) redirects to /api/
  # location = /api {
  #   return 301 /api/;
  # }

  # UI health endpoint that checks the private API health.
  # Public callers hit /health on the UI; Nginx proxies internally to the API's /api/health.
  # location = /health {
  #   proxy_pass http://api:8000/api/health;
  #}
}
