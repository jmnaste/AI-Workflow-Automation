server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Serve static SPA; API is routed by Traefik at the edge to the api service.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
