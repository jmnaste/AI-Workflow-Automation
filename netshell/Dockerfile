# NetShell image built from a maintained networking toolbox base
# Using wbitt/network-multitool (maintained successor of praqma/network-multitool)
FROM wbitt/network-multitool:latest

# Default: never exit — so you can exec into it any time
CMD ["sleep", "infinity"]
