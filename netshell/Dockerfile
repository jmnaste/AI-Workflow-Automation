# Optional: custom NetShell image you can publish to GHCR
# Base on a small multi-tool image for convenient diagnostics
FROM praqma/network-multitool:light

# Default: never exit — so you can exec into it any time
CMD ["sleep", "infinity"]
